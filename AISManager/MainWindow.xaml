<?xml version="1.0" encoding="UTF-8"?>
<Window x:Class="AISManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AISManager"
        mc:Ignorable="d"
        Title="AIS Update Manager"
        Height="700"
        Width="950"
        WindowStartupLocation="CenterScreen"
        Background="#F3F4F6"
        Closing="Window_Closing">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <!-- Стили кнопок -->
        <Style x:Key="MenuButtonStyle"
               TargetType="Button">
            <Setter Property="Background"
                    Value="Transparent" />
            <Setter Property="Foreground"
                    Value="#A0AEC0" />
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="FontSize"
                    Value="14" />
            <Setter Property="HorizontalContentAlignment"
                    Value="Left" />
            <Setter Property="Padding"
                    Value="20,12" />
            <Setter Property="Cursor"
                    Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver"
                         Value="True">
                    <Setter Property="Foreground"
                            Value="White" />
                    <Setter Property="Background"
                            Value="#38455A" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ActiveMenuButtonStyle"
               TargetType="Button"
               BasedOn="{StaticResource MenuButtonStyle}">
            <Setter Property="Background"
                    Value="#2A4365" />
            <Setter Property="Foreground"
                    Value="White" />
            <Setter Property="FontWeight"
                    Value="SemiBold" />
            <Setter Property="BorderBrush"
                    Value="#4299E1" />
            <Setter Property="BorderThickness"
                    Value="4,0,0,0" />
        </Style>
        <Style x:Key="PrimaryButtonStyle"
               TargetType="Button">
            <Setter Property="Background"
                    Value="#3182CE" />
            <Setter Property="Foreground"
                    Value="White" />
            <Setter Property="FontSize"
                    Value="14" />
            <Setter Property="FontWeight"
                    Value="Bold" />
            <Setter Property="Padding"
                    Value="25,10" />
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="Cursor"
                    Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver"
                         Value="True">
                    <Setter Property="Background"
                            Value="#2B6CB0" />
                </Trigger>
                <Trigger Property="IsEnabled"
                         Value="False">
                    <Setter Property="Background"
                            Value="#A0AEC0" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="230" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <!-- САЙДБАР -->
        <Border Grid.Column="0"
                Background="#1A202C">
            <StackPanel>
                <StackPanel Orientation="Horizontal"
                            Margin="20,30,20,5">
                    <TextBlock Text="AIS"
                               Foreground="#4299E1"
                               FontWeight="Bold"
                               FontSize="20" />
                    <TextBlock Text=" Manager"
                               Foreground="White"
                               FontWeight="Bold"
                               FontSize="20" />
                </StackPanel>
                <TextBlock Text="Automated Update System"
                           Foreground="#718096"
                           FontSize="11"
                           Margin="20,0,0,20" />
                <Separator Background="#2D3748"
                           Margin="0,0,0,10" />
                <Button x:Name="BtnFixes"
                        Content="📦  Пакеты исправлений"
                        Style="{StaticResource ActiveMenuButtonStyle}"
                        Click="MenuButton_Click" />
                <Button x:Name="BtnDistros"
                        Content="💿  Дистрибутивы ПО"
                        Style="{StaticResource MenuButtonStyle}"
                        Click="MenuButton_Click" />
                <Button x:Name="BtnSettings"
                        Content="⚙️  Настройки"
                        Style="{StaticResource MenuButtonStyle}"
                        Click="MenuButton_Click" />
            </StackPanel>
        </Border>
        <!-- КОНТЕНТ -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="80" />
                <RowDefinition Height="*" MinHeight="100" />
                <RowDefinition Height="Auto" /> <!-- Splitter -->
                <RowDefinition Height="{Binding LogHeight, Mode=TwoWay}" MinHeight="0" MaxHeight="400">
                    <RowDefinition.Style>
                        <Style TargetType="RowDefinition">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLogVisible}" Value="False">
                                    <Setter Property="Height" Value="0" />
                                    <Setter Property="MinHeight" Value="0" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </RowDefinition.Style>
                </RowDefinition>
            </Grid.RowDefinitions>
            <!-- ШАПКА -->
            <Border Grid.Row="0"
                    Background="White"
                    BorderBrush="#E2E8F0"
                    BorderThickness="0,0,0,1">
                <Grid Margin="30,0">
                    <StackPanel VerticalAlignment="Center"
                                HorizontalAlignment="Left">
                        <TextBlock Text="Текущая версия АИС Налог 3 Пром:"
                                   Foreground="#718096"
                                   FontSize="12" />
                        <TextBlock Text="{Binding CurrentVersion}"
                                   Foreground="#2D3748"
                                   FontWeight="Bold"
                                   FontSize="18"
                                   Margin="0,2,0,0" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center">
                        <Ellipse Width="10"
                                 Height="10"
                                 Fill="{Binding AutoCheckStatusColor}"
                                 Margin="0,0,10,0"
                                 VerticalAlignment="Center" />
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{Binding AutoCheckStatusText}"
                                       Foreground="{Binding AutoCheckStatusColor}"
                                       FontSize="12"
                                       FontWeight="SemiBold" />
                            <TextBlock Text="Обновлено: только что"
                                       Foreground="#A0AEC0"
                                       FontSize="10" />
                        </StackPanel>
                        <Button Command="{Binding ToggleLogCommand}"
                                Width="32"
                                Height="32"
                                Margin="20,0,0,0"
                                Background="White"
                                BorderBrush="#E2E8F0"
                                BorderThickness="1"
                                ToolTip="Показать/скрыть логи"
                                Cursor="Hand">
                            <TextBlock Text="📋"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center" />
                        </Button>
                        <Button Command="{Binding CheckUpdatesCommand}"
                                Width="32"
                                Height="32"
                                Margin="10,0,0,0"
                                Background="White"
                                BorderBrush="#E2E8F0"
                                BorderThickness="1"
                                ToolTip="Проверить обновления"
                                Cursor="Hand">
                            <TextBlock Text="↻"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center" />
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
            <!-- ГЛАВНАЯ ОБЛАСТЬ -->
            <Grid Grid.Row="1"
                  Margin="30,20">
                <!-- VIEW: FIXES -->
                <Grid x:Name="ViewFixes">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Список обновлений"
                                   FontSize="16"
                                   FontWeight="Bold" />
                        <CheckBox Grid.Column="1" 
                                  Content="Выбрать все" 
                                  IsChecked="{Binding IsAllSelected}"
                                  VerticalAlignment="Center"
                                  FontWeight="SemiBold"
                                  Foreground="#4A5568"/>
                    </Grid>
                    <ListView Grid.Row="1"
                              ItemsSource="{Binding Updates}"
                              BorderThickness="0"
                              Background="Transparent">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment"
                                        Value="Stretch" />
                                <Setter Property="Margin"
                                        Value="0,0,0,8" />
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListViewItem">
                                            <Border Background="White"
                                                    BorderBrush="#E2E8F0"
                                                    BorderThickness="1"
                                                    CornerRadius="8"
                                                    Padding="15,10">
                                                <ContentPresenter />
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="30" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="150" />
                                        <ColumnDefinition Width="120" />
                                    </Grid.ColumnDefinitions>
                                    <CheckBox IsChecked="{Binding IsSelected}"
                                              VerticalAlignment="Center" />
                                    <StackPanel Grid.Column="1"
                                                VerticalAlignment="Center">
                                        <TextBlock Text="{Binding FileName}"
                                                   FontWeight="SemiBold"
                                                   FontSize="14"
                                                   Foreground="#2D3748" />
                                        <TextBlock Text="{Binding Size}"
                                                   FontSize="12"
                                                   Foreground="#A0AEC0" />
                                    </StackPanel>
                                    <ProgressBar Grid.Column="2"
                                                 Value="{Binding ProgressValue}"
                                                 Maximum="100"
                                                 Height="6"
                                                 VerticalAlignment="Center"
                                                 Visibility="{Binding IsProcessing, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                    <Border Grid.Column="3"
                                            Background="{Binding StatusBgColor}"
                                            CornerRadius="12"
                                            Padding="10,4"
                                            HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding StatusText}"
                                                   FontSize="11"
                                                   Foreground="{Binding StatusFgColor}"
                                                   FontWeight="Bold" />
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <StackPanel Grid.Row="2"
                                Orientation="Horizontal"
                                HorizontalAlignment="Right"
                                Margin="0,20,0,0">
                        <Button x:Name="BtnProcess"
                                Content="⬇️ Скачать и создать FIX_№.exe"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Command="{Binding DownloadCommand}"
                                Width="240" />
                    </StackPanel>
                </Grid>
                <!-- VIEW: DISTROS -->
                <Grid x:Name="ViewDistros" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="Дистрибутивы ПО (FTP)" FontSize="16" FontWeight="Bold" Margin="0,0,0,15"/>
                    
                    <ScrollViewer Grid.Row="1">
                        <StackPanel>
                            <!-- ГРУППА OE -->
                            <Border Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="Последняя версия в OE:" Foreground="#718096" FontSize="12"/>
                                            <TextBlock Text="{Binding LatestDistro.Version, FallbackValue='Не проверено'}" FontWeight="Bold" FontSize="20" Margin="0,5,0,0"/>
                                            <TextBlock Text="{Binding LatestDistro.FileName}" Foreground="#A0AEC0" FontSize="11" Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <Button Grid.Column="1" Content="↻ Обновить OE" Command="{Binding CheckDistrosCommand}" Padding="15,5" Background="#EDF2F7" BorderBrush="#E2E8F0" BorderThickness="1" Cursor="Hand" VerticalAlignment="Top"/>
                                    </Grid>
                                    <Separator Margin="0,15" Background="#E2E8F0"/>
                                    <StackPanel>
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding LatestDistro}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <TextBlock Text="Статус:" Foreground="#718096" FontSize="12"/>
                                        <TextBlock Text="{Binding LatestDistro.DownloadStatus}" FontWeight="SemiBold" Margin="0,5,0,0"/>
                                        <ProgressBar Value="{Binding LatestDistro.Progress}" Maximum="100" Height="8" Margin="0,15,0,5" 
                                                     Visibility="{Binding LatestDistro.IsDownloading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                        <Button Content="⬇️ Скачать OE (.rar)" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding DownloadDistroCommand}" HorizontalAlignment="Left" Margin="0,15,0,0"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <!-- ГРУППА PROM -->
                            <Border Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="20">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="Последняя версия АИС Налог 3 (Пром):" Foreground="#718096" FontSize="12"/>
                                            <TextBlock Text="{Binding LatestAisProm.Version, FallbackValue='Не проверено'}" FontWeight="Bold" FontSize="20" Margin="0,5,0,0"/>
                                            <TextBlock Text="{Binding LatestAisProm.FileName}" Foreground="#A0AEC0" FontSize="11" Margin="0,2,0,0"/>
                                        </StackPanel>
                                        <Button Grid.Column="1" Content="↻ Обновить Пром" Command="{Binding CheckAisPromCommand}" Padding="15,5" Background="#EDF2F7" BorderBrush="#E2E8F0" BorderThickness="1" Cursor="Hand" VerticalAlignment="Top"/>
                                    </Grid>
                                    <Separator Margin="0,15" Background="#E2E8F0"/>
                                    <StackPanel>
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding LatestAisProm}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <TextBlock Text="Статус:" Foreground="#718096" FontSize="12"/>
                                        <TextBlock Text="{Binding LatestAisProm.DownloadStatus}" FontWeight="SemiBold" Margin="0,5,0,0"/>
                                        <ProgressBar Value="{Binding LatestAisProm.Progress}" Maximum="100" Height="8" Margin="0,15,0,5" 
                                                     Visibility="{Binding LatestAisProm.IsDownloading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                                        <Button Content="⬇️ Скачать Пром (.rar)" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding DownloadAisPromCommand}" HorizontalAlignment="Left" Margin="0,15,0,0"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                    
                    <TextBlock Grid.Row="2" Text="* Дистрибутив будет скачан в папку, указанную в настройках." Foreground="#A0AEC0" FontSize="11" Margin="0,10,0,0"/>
                </Grid>
                <!-- VIEW: SETTINGS -->
                <Grid x:Name="ViewSettings"
                      Visibility="Collapsed">
                    <ScrollViewer>
                        <StackPanel>
                            <TextBlock Text="Настройки"
                                       FontSize="18"
                                       FontWeight="Bold"
                                       Margin="0,0,0,20" />
                            <!-- БЛОК: ПАКЕТЫ ИСПРАВЛЕНИЙ -->
                            <Border Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,20">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                        <TextBlock Text="📦" FontSize="16" Margin="0,0,10,0"/>
                                        <TextBlock Text="Автоматизация фиксов" FontSize="15" FontWeight="Bold" Foreground="#2D3748"/>
                                    </StackPanel>
                                    
                                    <CheckBox IsChecked="{Binding IsFullAuto}"
                                              Content="Полная автоматизация (проверка обновлений и создание SFX)"
                                              Margin="0,0,0,12" />
                                    <CheckBox IsChecked="{Binding AutoDownload}"
                                              Content="Автоматически скачивать новые фиксы"
                                              Margin="0,0,0,5" />
                                    <TextBlock Text="При нахождении новых обновлений в КБ, они будут скачаны автоматически." 
                                               FontSize="11" Foreground="#718096" Margin="25,0,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- БЛОК: ДИСТРИБУТИВЫ -->
                            <Border Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,20">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                        <TextBlock Text="💿" FontSize="16" Margin="0,0,10,0"/>
                                        <TextBlock Text="Автозагрузка дистрибутивов (FTP)" FontSize="15" FontWeight="Bold" Foreground="#2D3748"/>
                                    </StackPanel>

                                    <CheckBox IsChecked="{Binding AutoDownloadDistro}"
                                              Content="Автоматически скачивать новый дистрибутив (OE)"
                                              Margin="0,0,0,12" />
                                    
                                    <CheckBox IsChecked="{Binding AutoDownloadAisNalog3}"
                                              Content="Автоматически скачивать АИС Налог 3 (Пром)"
                                              Margin="0,0,0,5" />
                                    <TextBlock Text="Приложение будет проверять наличие новых версий на FTP и скачивать их в указанные ниже папки." 
                                               FontSize="11" Foreground="#718096" Margin="25,0,0,0"/>
                                </StackPanel>
                            </Border>
                             <TextBlock Text="Папка выгрузки (архивы):"
                                        Foreground="#718096"
                                        Margin="0,0,0,5" />
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding DownloadPath, UpdateSourceTrigger=PropertyChanged}"
                                         Padding="5" />
                                <Button Grid.Column="1"
                                        Content="📁"
                                        Width="35"
                                        Margin="5,0,0,0"
                                        Command="{Binding SelectDownloadPathCommand}" />
                            </Grid>
                            <TextBlock Text="Папка для FIX_№.exe:"
                                       Foreground="#718096"
                                       Margin="0,0,0,5" />
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding SfxOutputPath, UpdateSourceTrigger=PropertyChanged}"
                                         Padding="5" />
                                <Button Grid.Column="1"
                                        Content="📁"
                                        Width="35"
                                        Margin="5,0,0,0"
                                        Command="{Binding SelectSfxOutputPathCommand}" />
                            </Grid>
                            <TextBlock Text="Папка для дистрибутивов (OE):"
                                       Foreground="#718096"
                                       Margin="0,0,0,5" />
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding DistroDownloadPath, UpdateSourceTrigger=PropertyChanged}"
                                         Padding="5" />
                                <Button Grid.Column="1"
                                        Content="📁"
                                        Width="35"
                                        Margin="5,0,0,0"
                                        Command="{Binding SelectDistroDownloadPathCommand}" />
                            </Grid>
                            <TextBlock Text="Папка для АИС Налог 3 (Пром):"
                                       Foreground="#718096"
                                       Margin="0,0,0,5" />
                            <Grid Margin="0,0,0,15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding AisNalog3DownloadPath, UpdateSourceTrigger=PropertyChanged}"
                                         Padding="5" />
                                <Button Grid.Column="1"
                                        Content="📁"
                                        Width="35"
                                        Margin="5,0,0,0"
                                        Command="{Binding SelectAisPromDownloadPathCommand}" />
                            </Grid>
                            <Separator Margin="0,10,0,20"
                                       Background="#E2E8F0" />
                            <TextBlock Text="Диагностика и данные"
                                       Foreground="#718096"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,10" />
                            <Button Content="📂 Открыть папку приложения (AppData)"
                                    HorizontalAlignment="Left"
                                    Padding="15,8"
                                    Background="White"
                                    BorderBrush="#CBD5E0"
                                    BorderThickness="1"
                                    Cursor="Hand"
                                    Command="{Binding OpenAppDataCommand}">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Foreground"
                                                Value="#4A5568" />
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver"
                                                     Value="True">
                                                <Setter Property="Background"
                                                        Value="#EDF2F7" />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                            <Button Content="🗑️ Очистить файлы логов"
                                    HorizontalAlignment="Left"
                                    Padding="15,8"
                                    Margin="0,10,0,0"
                                    Background="White"
                                    BorderBrush="#CBD5E0"
                                    BorderThickness="1"
                                    Cursor="Hand"
                                    Command="{Binding ClearLogsCommand}">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Foreground"
                                                Value="#4A5568" />
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver"
                                                     Value="True">
                                                <Setter Property="Background"
                                                        Value="#FFF5F5" />
                                                <Setter Property="Foreground"
                                                        Value="#C53030" />
                                                <Setter Property="BorderBrush"
                                                        Value="#FEB2B2" />
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>
            <!-- SPLITTER -->
            <GridSplitter Grid.Row="2"
                          Height="4"
                          HorizontalAlignment="Stretch"
                          Background="#CBD5E0"
                          Visibility="{Binding LogVisibility}"
                          ShowsPreview="True" />

            <!-- ЛОГ ПАНЕЛЬ -->
            <Border Grid.Row="3"
                    Background="#EDF2F7"
                    BorderBrush="#E2E8F0"
                    BorderThickness="0,1,0,0"
                    Visibility="{Binding LogVisibility}">
                <Grid>
                    <ScrollViewer x:Name="LogScroll">
                        <TextBox Text="{Binding LogOutput, Mode=OneWay}"
                                 TextChanged="TextBox_TextChanged"
                                 IsReadOnly="True"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 FontFamily="Consolas"
                                 FontSize="12"
                                 Padding="10"
                                 SpellCheck.IsEnabled="False"
                                 TextWrapping="Wrap" />
                    </ScrollViewer>
                    <Button Command="{Binding ClearLogsCommand}"
                            Content="Очистить логи"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top"
                            Margin="0,5,25,0"
                            Padding="8,2"
                            FontSize="10"
                            Background="White"
                            BorderBrush="#CBD5E0"
                            BorderThickness="1"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Foreground" Value="#718096" />
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#F7FAFC" />
                                        <Setter Property="Foreground" Value="#2D3748" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>